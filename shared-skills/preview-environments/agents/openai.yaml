interface:
  display_name: "Preview Environments"
  short_description: "Operate canonical dev previews and DevelopmentRuns."
  default_prompt: "Use $preview-environments to launch or inspect one exact dev PreviewEnvironment, operate its canonical host-owned DevelopmentRun through typed UI or MCP commands, follow bounded logs/traces/prompt/tool output, verify policy-owned delivery, and prove sealed evidence plus signed 12-check teardown."
