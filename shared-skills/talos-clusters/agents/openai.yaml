interface:
  display_name: "Talos Clusters"
  short_description: "Create, rebuild, resize, upgrade, and troubleshoot PittampalliOrg Talos/HCloud clusters and Crossplane spokes, including Tailscale API names and SWE-bench capacity gates."
  default_prompt: "Use $talos-clusters to plan or execute a Talos/HCloud spoke cluster change."
