# These pins match the bundled templates. Re-verify imports and component APIs
# against the target project's lockfile before changing the version line.
dapr-agents==1.0.5
dapr-ext-workflow>=1.18,<1.19
# Only needed for the MCP templates (FastMCP server and MCP client).
mcp>=1.26,<2
