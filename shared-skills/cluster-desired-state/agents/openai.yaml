interface:
  display_name: "Cluster Desired State"
  short_description: "Rebuild and audit hub, dev, and ryzen clusters."
  default_prompt: "Use $cluster-desired-state to plan, recreate, repair, or audit the requested cluster end to end."
