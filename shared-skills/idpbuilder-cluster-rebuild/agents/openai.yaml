interface:
  display_name: "Idpbuilder Cluster Rebuild"
  short_description: "Rebuild Ryzen with Talos and Podman"
  default_prompt: "Use $idpbuilder-cluster-rebuild to recreate the authoritative ryzen Talos Docker cluster through rootful Podman, then verify GitOps, images, Tailscale/API access, readiness, and kubeconfig access."
