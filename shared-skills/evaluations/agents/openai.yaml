interface:
  display_name: "Evaluations"
  short_description: "Build and debug Workflow Builder evaluations, code-eval harnesses, run payloads, and graders."
  default_prompt: "Use $evaluations to create or debug a Workflow Builder evaluation run."
