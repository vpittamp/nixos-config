interface:
  display_name: "Evaluations"
  short_description: "Run and debug evaluations and SWE-bench on dev."
  default_prompt: "Use $evaluations to launch, inspect, or debug a Workflow Builder evaluation or benchmark on dev."
