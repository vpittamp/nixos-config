interface:
  display_name: "Evaluations"
  short_description: "Run dev evals, Kimi K3 canaries, graders, and SWE-bench."
  default_prompt: "Use $evaluations to create or debug a Workflow Builder evaluation run on dev."
