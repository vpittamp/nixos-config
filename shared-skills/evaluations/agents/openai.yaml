interface:
  display_name: "Evaluations"
  short_description: "Operate canonical agent evaluation campaigns on dev."
  default_prompt: "Use $evaluations to launch, inspect, or debug a native, SWE-bench, or Harbor evaluation campaign on dev."
