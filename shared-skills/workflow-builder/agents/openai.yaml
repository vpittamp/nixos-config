interface:
  display_name: "Workflow Builder"
  short_description: "Author and inspect durable runs and evidence."
  default_prompt: "Use $workflow-builder to author, run, or debug an ordinary durable workflow or agent session, inspect its live trace or sealed evidence, and defer PreviewEnvironment lifecycle and canonical DevelopmentRuns to $preview-environments."
