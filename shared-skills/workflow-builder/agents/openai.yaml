interface:
  display_name: "Workflow Builder"
  short_description: "Author and debug durable Workflow Builder scripts."
  default_prompt: "Use $workflow-builder to author, save, run, or debug a durable Workflow Builder script or agent session."
