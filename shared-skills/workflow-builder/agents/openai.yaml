interface:
  display_name: "Workflow Builder"
  short_description: "Author and inspect durable runs and evidence."
  default_prompt: "Use $workflow-builder to author, run, or debug a durable workflow, host or preview run, agent session, live trace, or sealed execution-evidence package."
