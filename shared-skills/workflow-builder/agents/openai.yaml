interface:
  display_name: "Workflow Builder"
  short_description: "Author, debug, and inspect durable workflow evidence."
  default_prompt: "Use $workflow-builder to author, save, run, or debug a durable Workflow Builder script, agent session, live trace, or sealed execution-evidence package."
