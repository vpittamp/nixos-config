#!/usr/bin/env node
"use strict";

// Codex still supports a notify hook, but QuickShell session state now comes
// from Herdr directly. Keep the configured hook path valid without side effects.
process.exit(0);
